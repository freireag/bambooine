%h2= link_to h(post.title), post
%small= "by Thiago Freire " + time_ago_in_words(post.created_at) + " ago "
%small== | #{comments_string(post)}
-if logged_in?
  %small== | #{link_to "Edit", edit_object_path(post)}
  %small== | #{link_to "Delete", object, :method => :delete}
%br
%small= h(post.tag_list)
%p= textilize h(post.body)
%br
